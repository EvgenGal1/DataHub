{
  "version": 2,
  "builds": [
    {
      "src": "src/main.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["src/**/*"],
        "installCommand": "npm install"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.js",
      "methods": ["GET", "HEAD", "PUT", "PATCH", "POST", "DELETE"]
    }
  ]
}
